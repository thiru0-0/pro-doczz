# Example environment variables for resume-backend
# Copy this file to .env and fill in the values before running the backend.

# OpenAI API key (if used)
OPENAI_API_KEY=

# Google Generative AI key / project
GOOGLE_API_KEY=
GOOGLE_PROJECT_ID=

# Server port (optional, defaults to 3000 in many setups)
PORT=3000

# NODE_ENV=development
NODE_ENV=development
